[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
retry-rs = { path = "../.." }
retry-rs-macros = { path = "../retry-rs-macros" }

rand = "0.9.0"
tokio = { version = "1.43.0", features = ["full"] }