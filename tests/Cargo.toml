[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
retry-rs = { workspace = true }
retry-rs-macros = { workspace = true }

rand = "0.9.0"
tokio = { version = "1.43.0", features = ["full"] }